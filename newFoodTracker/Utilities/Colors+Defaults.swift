import SwiftUI

extension Color {
    // Fallback card gradient
    static let cardStart = Color(red: 0.95, green: 0.60, blue: 0.20)
    static let cardEnd   = Color(red: 0.90, green: 0.30, blue: 0.10)
    // Fallback background gradient
    static let backgroundStart = Color(red: 0.90, green: 0.90, blue: 0.95)
    static let backgroundEnd   = Color(red: 0.80, green: 0.85, blue: 0.90)
}
